# FastAPI and web server
fastapi==0.104.1
uvicorn==0.24.0
starlette==0.27.0
pydantic==2.11.9
pydantic_core==2.33.2

# Kafka client
confluent-kafka==2.3.0

# AWS SDK for S3
# s3fs and fsspec are version-locked together — pin both to the same release,
# otherwise pip resolves a newer s3fs that conflicts with the fsspec pin.
boto3==1.40.69
fsspec==2025.12.0
s3fs==2025.12.0

# Data processing
pandas==2.1.4
numpy==1.26.4

# Machine Learning
scikit-learn==1.7.2
joblib==1.5.2
# Pinned old: shap>=0.45 requires numpy>=2, which conflicts with the numpy==1.26.4
# pin above (and would silently change how the pickled model unpickles/behaves).
shap==0.44.1

# Transaction status tracking (writes to the shared Postgres instance owned
# by fraud-rag's Alembic migrations — this service only ever touches the
# transaction_status table, never `transactions`/`transaction_embeddings`).
SQLAlchemy==2.0.46
psycopg2-binary==2.9.11

# Configuration
PyYAML==6.0.3
python-dotenv==1.0.0

# Testing
pytest==8.3.5
httpx==0.27.0
